# keywords-manager
SEO/GEO keywords manager skills

Default runtime data lives in `~/.data/keywords-manager/keywords.db`. Use `KEYWORDS_MANAGER_DB`, `KEYWORDS_MANAGER_DATA_DIR`, or `--db-path` to move it elsewhere (and to share a single database across different tools or skill installs).
